namespace SLSKDONET.Views;

/// <summary>
/// Local notification type enumeration used by the app as an abstraction
/// (keeps implementations decoupled from Wpf.Ui internals).
/// </summary>
public enum NotificationType
{
    Information,
    Success,
    Warning,
    Error
}
